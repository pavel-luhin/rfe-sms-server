(function () {
    'use strict';

    angular
        .module('sms-server')
        .factory('setupCredentialsService', setupCredentialsService);

    setupCredentialsService.$inject = [];
    function setupCredentialsService() {

    }
})();